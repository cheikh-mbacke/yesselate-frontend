/**
 * Contexts Index
 * ==============
 * 
 * Export centralisé de tous les contextes
 */

export { AuthProvider, useAuth, ProtectedRoute, UserAvatar } from './AuthContext';
export type { User } from './AuthContext';

